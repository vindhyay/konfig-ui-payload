import { FindStylesPipe } from './find-styles.pipe';

describe('FindStylesPipe', () => {
  it('create an instance', () => {
    const pipe = new FindStylesPipe();
    expect(pipe).toBeTruthy();
  });
});

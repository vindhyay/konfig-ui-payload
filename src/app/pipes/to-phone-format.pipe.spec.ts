import { ToPhoneFormatPipe } from './to-phone-format.pipe';

describe('ToPhoneFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ToPhoneFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
